{
  "name": "aurea-ecommerce",
  "version": "1.0.0",
  "description": "Aurea — Premium Herbal Skincare Monorepo (FE + BE + Infrastructure)",
  "private": true,
  "scripts": {
    "start:fe": "cd frontend && ng serve",
    "start:be": "cd backend && npm run dev",
    "build:fe": "cd frontend && ng build --configuration production",
    "build:be": "cd backend && npm run build",
    "install:all": "cd frontend && npm install && cd ../backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
